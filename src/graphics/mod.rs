mod instance;
mod output;
mod device;

pub use instance::*;
pub use output::*;
pub use device::*;

use std::iter::once;

use winit::window::Window;

pub struct GraphicsSystem {
}

impl GraphicsSystem {
}
